[pytest]
testpaths = tests/test_api_server_units.py
addopts = -v --tb=short --cov=api --cov-report=term-missing --cov-fail-under=95
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
